import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,

    alignItems: "center",
  },
  gradientColors: ["#f94144", "#f3722c", "#f8961e", "#f9c74f"],
  imageStyle: { opacity: 0.25, flex: 1 },
});
